# Portable Paper Collage Workspace

This workspace is created by the installed Skill when the full Remotion repository is unavailable. Project state lives under `projects/<slug>/`. Use the Skill scripts to validate and build a self-contained HTML preview.

The portable runtime is intentionally honest: it does not claim MP4, Chromium, Remotion, provider, or semantic-vision validation.
