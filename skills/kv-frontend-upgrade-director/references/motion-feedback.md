# 动效反馈系统

每个动作定义：视觉变化、声音、状态、时长、失败降级。

## 原则

- 动画解释状态。
- 元素有来源和去向。
- 高频反馈短，状态变化稍长，结算可以展开。
- 支持 reduced-motion。

## 常用模式

进入：透明度 + 位移。
选择：颜色 + 微位移。
提交：锁定 + 进度变化。
成功：聚合、释放、上扬。
失败：克制提示，不羞辱。

声音：首次用户操作后解锁 AudioContext。
