# 模块 91：弱模型分轮协议

不要一轮同时重构业务、视觉、动效和部署。按顺序执行：

1. 只读项目，建立功能冻结和问题队列；
2. 只写视觉命题、Token 和两个 Showcase 方案；
3. 只实现共享 Shell、组件和一个 Showcase；
4. 只加入必要的语义动效或创意层；
5. 只处理手机、矮屏、性能和失败降级；
6. 只做浏览器回归、修复和交接。

每轮明确保留项、只改项、禁止项和验收项。
