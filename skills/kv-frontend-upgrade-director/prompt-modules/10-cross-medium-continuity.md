# 模块 10：跨媒介连续性

### 适用

希望输入、卡片、文本或按钮自然“进入”三维场景，而非突然切换。

### 提示词

```text
为 DOM 与 WebGL 之间建立知觉连续性。

当用户触发核心动作时：
1. 读取 DOM 对象的屏幕坐标；
2. 生成临时字符、碎片、光点或轮廓；
3. 沿可理解路径飞向 WebGL 主对象；
4. 在到达时触发空间对象汇聚、变形或点亮；
5. 清理临时 DOM；
6. 最终将空间结果收束回可操作的 DOM 工作区。

用户应感觉是“同一个东西发生转换”。不要同时播放两段互不相关的动画。

控制临时节点数量，动画结束后必须清理。支持快速重复操作与 reduced motion。
```

---
